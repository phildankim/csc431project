fun f (int x, int y) int
{
	int sum;

	if (x >= y)
	{
		return 0;
	}

	sum = 0;

	while (x < y)
	{
		if (x / 2 * 2 == x)
		{
			sum = sum + 1;
		}

		x = x + 1;

	}

	return sum;
}

fun main() int
{
	f(1,10);

	return 0;
}