fun f (int x, int y) int
{
	int sum;

	if (x >= y)
	{
		return 0;
	}

	sum = 0;

	while (x < y)
	{
		if (x / 2 * 2 == x)
		{
			sum = sum + 1;
		}

		x = x + 1;

	}

	return sum;
}

fun main() int
{
	int a, b;

	a = read;
	b = read;

	print f(a,b);

	return 0;
}